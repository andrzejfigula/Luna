══════════════════════════════════════════════════════════════════
  Luna — Update Log
  Every change to the project gets an entry here.
  Format: date · area · what changed · why
══════════════════════════════════════════════════════════════════

─── 2026-09-23 — Occasions pack (10 scenes) ─────────────────────────────

[registry] Two additions so the calendar can drive her: dates=("MM-DD",
  "MM-DD") windows (wrapping the new year), a per-scene available()
  predicate for anything else, and forced=True — a scene flagged so plays
  as soon as it is available instead of waiting for the random scene
  timer, which is what a New Year or a birthday needs. idle_engine checks
  for those every tick, with FORCED_COOLDOWN between repeats.
[idle] morning (the sun comes up and she wakes with it, 5-10), sunset
  (18-21), night_sky (moon and twinkling stars, 21-5), season (a snowflake
  / flower / sun / leaf, by month), hour_chime (counts 3-2-1 into the full
  hour, then shows it — fires 5-9 s before the hour), birthday (cake, she
  blows the candles out; dates from LUNA_BIRTHDAYS in .env), santa_hat
  (December), pumpkin (24-31 Oct), fireworks (New Year's Eve, forced) and
  valentine (13-14 Feb).
[note] "Weather" from the original list was dropped: without a network
  call there is no honest way to know it, and inventing it would be worse
  than not having it. Valentine's Day took its place.

─── 2026-09-23 — Emotions pack (13 scenes) ──────────────────────────────

[face] Mouth driving generalised: the yawn-only hack became
  face._mouth_drive, which any scene may set in motion(), so laughing,
  sighing and exhaling open her mouth the way speech does.
[idle] laugh (shaking, mouth going, tears of mirth), sigh, impatient
  (drumming fingers), think_bubble (three dots rising), proud, shy
  (hiding behind a hand, blushing), behind_you (snaps round to look),
  scared (trembling, a bead of sweat), anger_cools (fumes, then lets it
  go with a breath), dance, tear_wipe (a tear, then she wipes it),
  relief, curious (head right over, question mark). Scenes that wear a
  mood get the matching particles for free — sad gives tears, angry
  steam, excited the starburst.
[brain] The reply vocabulary is now grouped by what it expresses rather
  than listed, so it stays readable as it grows: 32 gestures available.
  Verified live: a joke gets a wink, "my dog is sick" gets tear_wipe,
  "how many stars are there" gets think_bubble.
[perf] All hold ~30 fps on the Pi (dance 28).

─── 2026-09-23 — Everyday props pack ────────────────────────────────────

[idle] coffee (steam curling up, she tilts the mug and sips, eyes closing
  — daytime only), camera (held in both hands, the bulb lights, then a
  full-screen flash through post() and she blinks at her own flash),
  binoculars (tinted lenses over her eyes, scanning the room, head
  panning with them), spinner (spins down over the scene), yoyo (drops
  and returns on its string, eyes tracking it) and hourglass (sand runs
  out, then she flips it over).
[registry] Scenes may declare hours=(from, to) and only come up inside
  that window — used for coffee, and what the seasonal/occasion scenes
  will need. Catalogue is now 41 scenes, 21 of them usable by the LLM.

─── 2026-09-23 — Screen pack: the panel itself as a medium ───────────────

[face] Two new scene hooks: draw_bg() paints BEHIND her face and post()
  works on the finished frame, so a scene can own the whole screen.
[idle] rain (drops on the glass, she watches them), snow, leaves,
  dvd_logo (the screensaver, bouncing, eyes tracking it, colour changes
  on every bounce), matrix (glyph columns behind her), hyperspace (stars
  streaming from the centre) and glitch (the frame tears into displaced
  bands — also usable in a reply). All weighted up at night, where they
  work as a wallpaper.
[perf] Measured on the Pi: every one of them holds 30 fps. Two things
  that would have cost: katakana in matrix rendered as empty boxes in the
  system font (ASCII now) and copying ~190 glyph surfaces per frame
  (set_alpha on the cached surface instead).

─── 2026-09-23 — Hands pack: eight scenes that use her hands ─────────────

[idle] Scenes can now drive the hands: the gesture block hands a mutable
  idle_scenes.Hands (target x/y/angle and pose per side) to the scene,
  which may override it. New "fist" hand pose alongside open/thumb.
  Scenes: clap, wave_both, peekaboo (eyes shut behind the hands, then
  thrown open wide), rub_eyes (fists, sleepy — weighted up at night),
  scratch_head, chin_rest, salute, please (with hearts). Six of them are
  in_reply, so the LLM can use them as body language: 14 scenes are now
  available to it, out of 28 in the catalogue.

─── 2026-09-23 — Scene registry + 11 new idle scenes + scenes in replies ──

[refactor] idle_scenes.py: every idle behaviour is now a small class
  registered with @scene(name, duration, weight, night_weight, mood,
  needs_face, in_reply), implementing any of motion() / eyes() / draw().
  robot_face just looks the scene up and calls it — the elif chains in
  update()/draw() and the _draw_* methods are gone, and adding a
  behaviour no longer means editing four places. Weights, durations and
  moods live with the scene; config keeps IDLE_SCENE_WEIGHTS /
  IDLE_SCENES_DISABLED as tuning overrides.
[idle] Eleven new eyes-only scenes (the cheap ones that carry most of the
  liveliness): double_blink, slow_blink, cross_eyes, eye_roll,
  eye_twitch, remember, peek, staring, bored, smirk, fly (a fly buzzes
  around, she tracks it and blinks it away). Per-eye pupil offset added
  for cross_eyes.
[brain] The LLM may now pick a facial scene as body language, not just
  hand gestures: wink, smirk, eye_roll, remember, cross_eyes,
  slow_blink, double_blink, eye_twitch (scenes marked in_reply=True;
  prop scenes stay out so nothing fights lip sync). The enum is built
  from the registry, so marking a new scene in_reply is all it takes.

─── 2026-09-23 — Idle scene: listening to music ─────────────────────────

[idle] "music": headphones slide on (ellipse-arc headband meeting two ear
  cups), quavers drift up beside her, head bobs and sways to ~114 bpm and
  the eyes settle into a contented squint that pulses with the beat.
  IDLE_DURATION moved to config.py so idle_engine can use it too, and
  IDLE_SCENE_MOODS lets a scene wear a mood — music wears "happy", so the
  smile and blush come along.

─── 2026-09-23 — More idle scenes + the mouth no longer vanishes ─────────

[face] Mouth.draw branched on `speaking`, and the "not speaking" branch
  draws NOTHING for a neutral face — so the mouth popped out of existence
  in a single frame when a reply ended. Mouth now keeps a speak_fade that
  decays after speech: talk_open eases to 0 (lips close) and the shape
  fades over ~1.2 s. The CLOSED line shape was also painted straight onto
  the canvas with no alpha, so it stayed at full brightness forever —
  it is drawn on its own surface now.
[idle] Three new prop scenes: read_book (open book at the bottom, eyes
  scanning each line, one page flipping over), phone (tilted handset,
  content scrolling, eyes down-right) and ball (bounces below the face,
  squashes on impact, eyes and head track it). Props ease in and out via
  _prop_hold(). Weighted into IDLE_SCENES / IDLE_SCENES_NIGHT.

─── 2026-09-22 — Idle life + touch: Luna notices you and can be petted ──

[idle] idle_engine.py: presence is tracked with a debounced signal (raw
  face_detected flickers several times a minute and reset every timer).
  Coming back after IDLE_ABSENCE_SECS gets a happy face, a wave and —
  budget permitting — a spoken greeting picked by time of day, with a
  separate line the first time she sees you that day. Between
  conversations she plays visual micro-scenes (wink / look_around /
  stretch / yawn / clock), weighted differently after NIGHT_FROM.
  Unprompted speech is the only rate-limited part (PROACTIVE_MIN_GAP_SECS,
  quiet hours, "Luna, cicho" mutes for an hour); animations are free.
  The absence timer is stamped before the deferred text_to_speech import,
  which costs ~4 s building the TTS engine.
[touch] touch_module.py: SDL/Wayland delivers NO touch events to the
  pygame window (verified: no FingerDown/MouseButtonDown), so the panel's
  evdev node is read directly — works regardless of focus, needs the user
  in the "input" group. Taps, strokes and repeated pokes are recognised;
  robot_face maps the point to a face zone (eye / mouth / top / other)
  using the live face geometry and answers instantly (recoil, blink,
  squint, bounce, love/angry face), idle_engine adds the occasional line.
[face] Per-eye blink so she can wink; yawn drives the mouth; stretch
  narrows the eyes; look_around sweeps head and pupils; the clock scene
  floats the time below the eyes with a glow that follows the digits.

─── 2026-09-21 — Far waves in daylight: presence scaling, CLAHE faces ───

[gesture] From further away the small hand's motion blob flickers at the
  turns, so the presence threshold now scales with face width (0.6 at a
  40 px face → floor 0.35); motion mask more sensitive (diff 22→16, min
  blob 25→12 px²). Hand-height limit 1.3 (waving from the elbow).
[vision] Backlit far faces (window behind the user) were found only
  ~2×/50 s: CLAHE local contrast instead of global equalisation and
  minNeighbors 3→2 → ~10×/50 s. VISION_DEBUG flag logs detections.

─── 2026-09-21 — Wave detection in daylight ─────────────────────────────

[gesture] In daylight the face-derived skin model collapsed (uniform face
  → tiny std → window too narrow) and real waves scored 0.27-0.45 skin.
  Measured four tolerance variants side by side: with any of them, skin
  does NOT separate an empty hand from a hand with an object in daylight.
  What does, consistently across night / day / distance, is the hand's
  HEIGHT relative to the face (waves -0.7..+0.7 half face heights,
  showing an object +0.8..+1.9) → WAVE_MAX_BELOW_FACE 1.2 → 0.75 is the
  key rule again; skin kept as a weak wide filter (sigma 3.0, std floor
  6, threshold 0.60 scaled by distance, floor 0.45).

─── 2026-09-21 — Wave detection: scale-free thresholds ──────────────────

[gesture] Amplitude, swing and speed thresholds are now fractions of the
  detected face width (were absolute px, so waves from further away had
  half the pixel travel and were dropped). The skin threshold scales with
  distance too: 0.75 at a 40 px face, down to a 0.50 floor (a small far
  hand's box holds more background; measured far waves 0.55-0.70). Hand
  height tolerance 0.7 → 1.2 (people wave lower from the elbow when
  further away). Verified 6/6 waves at distance, objects rejected.

─── 2026-09-21 — Wave detection tuned on real data ────────────────────────

[gesture] Logged every candidate's features (presence, spans, speed,
  reversals, blob area, position vs. face, skin fraction) while the user
  waved / showed objects / adjusted glasses & headphones. Findings:
  • the vertical-vs-horizontal ratio and swing-regularity rules rejected
    REAL waves (the moving blob is hand + forearm; 15 fps sampling makes
    swings uneven) → removed;
  • 15 fps analysis (was 10) and a 1.5 s face-grace window fixed most
    missed waves (Haar drops frames while the hand moves near the head);
  • showing an object vs. waving can't be told apart by motion at all —
    the discriminator is SKIN: the person's own Cr/Cb skin tone is
    sampled from the face box every frame and the moving blob must be
    ≥ 65 % skin (measured: empty hand 0.72-0.94, hand + object 0.19-0.58).
  • cloud confirmation via the vision model was tried and dropped (~1 s
    delay, missed real waves); WAVE_CLOUD_CONFIRM stays as an off switch.
  Final: face seen ≤1.5 s ago, hand beside the head (1.15-7 half-widths),
  ≥4 reversals of ≥5 px in 1.6 s, presence ≥0.6, speed ≥4 px/sample,
  skin ≥0.65. Verified 4/4 waves, 0/4 objects, 0 false positives on
  glasses / headphones / raising a hand.

─── 2026-09-20 — Luna always sees: camera frame on every request ────────

[brain] VISION_ALWAYS: low-detail frame attached to every request; the
  (expanded, PL+EN) VISION_KEYWORDS now only pick high detail (reading
  text, small objects). Persona states that her sight is the Logitech
  webcam on the Raspberry Pi and that "zobacz / przeczytaj / jaki kolor /
  look / read" refer to that picture; never claim to be unable to see.

─── 2026-09-20 — Gestures: wave detection, nod/shake, hair + hands ──────

[gesture] MediaPipe is unusable on the Pi 4 + Python 3.13 (its 1.0.x
  arm64 wheels need ARMv8.2 instructions → SIGILL on the Cortex-A72; no
  older version has a 3.13 wheel). gesture_module.py rewritten as
  OpenCV frame-differencing at 160x120: hand-sized moving blob near the
  face whose x reverses direction ≥3 times within 1.4 s = WAVE. ~1 ms per
  frame at 10 fps. behavior_engine.py (no servos): Luna waves back with
  her hand + happy face + greeting (silent inside a conversation window).
[brain] Reply JSON gains "gesture": none | nod | shake | wave | thumbs_up
  | heart, with rules tied to the answer word (a reply starting with
  "Nie"/"No" is always shake). state.gesture_anim / gesture_anim_start
  drive the animation; GESTURE_DURATION in config.
[face] Style 3 extras: HAIR (fringe of 5 gradient tufts + cowlick, sways
  with the head) and HANDS (gradient mitten hands, open / thumb poses,
  rendered once per pose and cached; tucked below the screen at rest).
  Animations: nod (vertical bob), shake (horizontal + slight tilt), wave
  (right hand up, rocking), thumbs_up (fist + thumb, bobbing), heart
  (both hands at the chin + pulsing heart). Shape-following bloom on
  hands. FACE_Y_OFFSET -28 → -14 to make room for the hair. 30 fps kept.

─── 2026-09-18 — Mic sensitivity ────────────────────────────────────────

[stt] The C270's hardware capture gain was already at max (+30 dB) and
  PortAudio bypasses PipeWire's source volume, so: MIC_GAIN=1.6 software
  gain on the mic stream (int16, clipped) before the gate/Vosk/cloud, and
  a gentler gate (MIC_ENERGY_THRESHOLD 500→400, MIC_GATE_FACTOR 2.5→2.0).
  Speech now sits 3-4x above the gate instead of ~2x.

─── 2026-09-18 — Real lip sync ───────────────────────────────────────────

[tts] openai_tts.Envelope: every PCM chunk sent to the player is reduced
  to 20 ms RMS frames (0..1, perceptual curve); envelope.start() stamps
  the wall clock when the player begins consuming byte 0 (after the
  prebuffer). text_to_speech._energy_loop reads envelope.energy_at(now -
  LIPSYNC_LATENCY_MS) with asymmetric attack/release smoothing; the old
  sine-mix is kept only as a fallback. Mouth floor 0.15 → 0.04 so pauses
  actually close the lips. Verified on the Pi: "Raz. Dwa. Trzy." shows
  three bursts with silence between, word-level modulation after.

─── 2026-09-18 — Style 3 eyebrows: domed gradient + bloom ───────────────

[face] gradient_sheet() (unmasked domed shading, shared with the eyes)
  tints the brow shape; bloom() gives a shape-following glow via a cheap
  shrink/enlarge blur. Brow layer is tight around the brow so the
  gradient spans the brow itself; brows sit 30 px above the eye, 15 px
  thick. 30 fps unchanged.

─── 2026-09-18 — Style 3 depth: domed gradient, pupils, gradient brows ──

[face] gradient_block now numpy-generated: vertical gradient × radial
  falloff (bright core above centre, darker rim) + specular sheen in the
  upper third — reads as a back-lit domed panel. BLOCK_PUPIL: dark rounded
  pupil with catch-light inside the block, drifts with the glance, clipped
  by the shape cuts. Eyebrows drawn on a layer and tinted with a vertical
  gradient (BROW_GRAD_TOP/BOTTOM) + faint glow. Still 30 fps on the Pi.

─── 2026-09-18 — Face style 3 "loona" ──────────────────────────────────────

[face] New STYLES[3]: amber palette, EYE_MODE="block" — pupil-less rounded
  blocks with a vertical gradient (gradient_block, cached), the whole block
  glances instead of a pupil, expressions carried by the eye shape: happy →
  bottom cut into an upward arc, sad → outer top corners sloped, angry →
  inner top corners sloped, squint lowers the lid. Cuts are done in the
  eye's own layer so glow/blush behind stay intact. Per-style EYE_SCALE /
  EYE_SPREAD (bigger, closer eyes); width capped so blocks never collide.
  Brows, mouth, blush, hearts, tears, Zzz all kept. Key 3 switches live.
  FACE_STYLE default 2 → 3.

─── 2026-09-18 — Mid-sentence crackle, real root cause: 128-sample quantum ──

[audio] Bisected by ear: clean with Luna stopped, clean with mic+Vosk+camera
  only, crackly with the face renderer only. The renderer is cheap (~7 ms
  per frame) — the problem was that PipeWire ran the 3.5 mm sink at a
  128-sample quantum (2.7 ms), so every display page-flip from the renderer
  caused an ALSA xrun (pw-top ERR counter climbing during playback).
  pi/51-luna-quantum.conf pins the graph to a 1024-sample quantum, and
  the WirePlumber rule adds api.alsa.headroom=1024. Verified: 0 xruns
  over an 18 s playback with the face animating at 30 fps.
  Power (get_throttled) was checked and ruled out for this symptom.

─── 2026-09-18 — Mid-sentence crackle: PipeWire had no realtime priority ──

[audio] Root cause: rtkit not installed and the user not in the "pipewire"
  group, so PipeWire's data-loop ran as a normal timeshare thread and got
  preempted by Luna's render/Vosk threads → sink underruns. Fix: rtkit +
  usermod -aG pipewire (documented in SETUP.md), plus run.sh now starts
  Luna with nice 10 as a safety net. The stream itself was never the
  problem (arrives 8-10x realtime).

─── 2026-09-18 — Start-of-reply crackle on the 3.5 mm jack ─────────────

[audio] The BCM2835 analog output pops when its stream (re)opens after
  PipeWire's idle suspend. Two layers: pi/51-luna-no-suspend.conf
  (WirePlumber rule, session.suspend-timeout-seconds=0 for ALSA sinks,
  installed by install_autostart.sh) and TTS_LEADIN_SECS of silence
  prepended to every reply so any residual pop lands before the voice.

─── 2026-09-18 — Wake-word reliability, TTS prebuffer ────────────────────

[stt] Vosk-small mishears "Luna" a lot ("nie", "cudowna", "zdaniem"...).
  • REQUIRE_FACE_TO_TALK now False — with it on, "Luna!" was silently
    ignored whenever the camera hadn't seen a face in the last 4 s.
  • Thresholds lowered: STT_CONFIDENCE 0.55→0.45, WAKE_CONFIDENCE
    0.60→0.45, WAKE_FUZZY_RATIO 0.80→0.70; Polish inflections of the
    name added to WAKE_WORDS.
  • CLOUD_WAKE_CHECK: in passive mode, speech that Vosk heard without a
    wake word (or dropped as low-confidence but clearly loud) is sent to
    the cloud and the wake word is looked for in that transcript.
    Rate-limited to one check per CLOUD_WAKE_MIN_INTERVAL seconds.
[tts] TTS_PREBUFFER_SECS (0.6 s) of audio is collected before the player
  starts so a network stutter no longer underruns the sink (crackle).
  Stream chunk size 4 KB → 16 KB.

─── 2026-09-18 — .env hardware options, jack output, brighter voice ─────

[config] LUNA_CAMERA / LUNA_MIC / LUNA_SPEAKER / LUNA_TTS_VOICE read from
  .env (empty = auto). LUNA_MIC accepts a name substring ("C270");
  LUNA_CAMERA accepts a /dev/videoN path; LUNA_SPEAKER accepts jack /
  hdmi / bluetooth / default or a PipeWire node name — resolved through
  pw-dump in openai_tts.py and passed to pw-play --target.
[tts] Default voice coral → nova with bright/friendly/upbeat instructions.

─── 2026-09-18 — Raspberry Pi 4 desktop-robot fork (OpenAI, no servos) ──

[deps] Python 3.13 / Pi OS Trixie / arm64. Removed torch, tensorflow,
  deepface, mediapipe, sentence-transformers, groq, opencv-contrib, Piper.
  Left: pygame+numpy+opencv (apt), sounddevice, vosk, openai.
[removed] gesture_module.py, behavior_engine.py, servo_module.py,
  piper_tts.py, models/, test.py, SERVO_SETUP.md, pi_inst.txt.
[brain] Groq → OpenAI gpt-4.1-mini. Reply is JSON {reply, emotion}; the
  LLM's emotion is written to state.emotion (frozen for the utterance by
  text_to_speech) and then lingers as face_override — robot_face untouched.
  Camera frame attached (JPEG, detail=low) when the question matches
  VISION_KEYWORDS (PL/EN). Real local date/time injected per request.
[stt] Vosk (small Polish model) keeps wake word, energy gate, endpointing
  and confidence gates; the buffered utterance goes to
  gpt-4o-mini-transcribe for the final text (Vosk text as fallback).
[tts] openai_tts.py: gpt-4o-mini-tts streamed as raw PCM into pw-play —
  same pump / on_audio_start lip-sync contract piper_tts.py had. pw-play
  (not aplay) because pipewire-alsa isn't on the stock image, so ALSA
  default bypasses PipeWire and the Bluetooth speaker.
[vision] Haar face tracking only (eye-follow + addressed-speech gate),
  on a half-size copy of the 640x480 frame; cascade XML bundled in data/
  because Debian's python3-opencv has no cv2.data.
[face] 800x480 fullscreen on Wayland (SDL wayland driver, 30 fps), cursor
  hidden, face lifted 28 px and awake-dot moved below the mouth on small
  panels, new "thinking" state (luna_mode == "processing"), ESC quits.
[camera] cv2.CAP_V4L2 + MJPG — opens in 1 s instead of ~6 s via GStreamer.
[run] run.sh / stop.sh / install_autostart.sh (labwc autostart + lwrespawn
  watchdog; graphical-session.target is inactive under labwc so a systemd
  user unit would not fire). main.py exits with os._exit after pygame.quit
  so OpenCV/PortAudio daemon threads don't abort at interpreter teardown.

─── 2026-07-15 — Fix: Luna hearing/replying to her own voice ─────

[bug] Real feedback loop observed live: Luna would finish an answer,
  the mic would pick up her own voice through the speaker, Vosk would
  transcribe it (sometimes verbatim, sometimes badly garbled), and
  she'd reply to herself — spiraling into nonsense over several turns.
  Root cause: the previous mic-blocking rewrite (see below) trusted
  state.speaking to cover the whole physical speech duration. On
  macOS, pyttsx3's "nsss" driver can return from runAndWait() BEFORE
  the audio has actually finished playing — especially with a
  persistent/reused engine called repeatedly from a background
  thread, which is exactly this project's setup. state.speaking flips
  False (and the mic unblocks) while Luna is still audibly talking.

[speech] text_to_speech.py
  • speak() now measures wall-clock elapsed time against the expected
    speech duration. If the engine returned suspiciously early
    (elapsed < TTS_EARLY_RETURN_RATIO × expected), the mic block is
    padded with the remaining expected playback time on top of the
    normal MIC_BLOCK_AFTER_SPEAK guard — restores real protection
    without permanently re-adding the old flat "block for the whole
    reply regardless" tax on platforms where the driver blocks
    correctly (Pi espeak subprocess, Windows SAPI5).
  • Records state.last_spoken_text / state.last_spoken_time on every
    utterance for the new echo filter (below).

[core] main.py
  • Second, independent layer of defense: _is_self_echo() compares
    newly heard text against Luna's last spoken reply and discards it
    if BOTH the longest contiguous word-run AND overall word overlap
    clear a threshold (ECHO_RUN_THRESH / ECHO_OVERLAP_THRESH in
    config.py). Requiring both (not either) keeps it high-precision —
    tested against the real transcript that exposed this bug plus a
    battery of plausible new short questions sharing common words
    ("can", "you", "i"...) with Luna's reply, with no false positives.
    Catches near-verbatim echo; heavily garbled multi-generation
    echoes are left to the timing fix above, which prevents most of
    them from reaching the recognizer in the first place.

[config] config.py — new TTS_EARLY_RETURN_RATIO, ECHO_GUARD_WINDOW,
  ECHO_RUN_THRESH, ECHO_OVERLAP_THRESH.

[state] shared_state.py — new last_spoken_text, last_spoken_time.

─── 2026-07-15 — Conversation-flow + face-style update ───────────

[speech] text_to_speech.py
  • speak() is now serialized with a lock:
      speak(text)                → waits its turn — a brain answer is
                                   NEVER silently dropped (old code
                                   returned if already speaking, so a
                                   gesture reaction could eat the answer)
      speak(text, can_drop=True) → optional chatter (gestures), skipped
                                   if Luna is busy.
  • FIXED double mic block: the mic was re-blocked for the full
    estimated speech length AFTER the (synchronous) speech already
    played — up to ~10 s of dead mic per long reply. Now a short
    MIC_BLOCK_AFTER_SPEAK echo-guard (0.6 s) is used; speech itself
    is covered by state.speaking.
  • FIXED duplicate talking-mouth drivers: a per-call energy thread
    fought the module-level one, both writing audio_energy every
    16 ms → jittery mouth. Single driver remains.
  • Conversation window now refreshes when Luna FINISHES speaking —
    her own reply no longer eats the user's CONVO_TIMEOUT.

[speech] speech_to_text.py
  • KaldiRecognizer created once and Reset() per turn (was allocated
    every listen cycle — needless CPU/alloc churn on the Pi).
  • POST_SPEAK_DELAY (0.8 s) now only applies right after Luna spoke;
    it used to be added to EVERY listen cycle, even silent ones.
  • Conversation timeout is also detected mid-listen (silence chunks
    and queue idle), not only at the start of a cycle; expiry stamps
    state.convo_expired_time so the face can show the rest cue.
  • Wake word heard alone ("Luna!") returns WAKE_ACK — main.py answers
    with a short random acknowledgement (WAKE_REPLIES in config)
    instead of doing nothing. Also fixes luna_mode getting stuck on
    "processing" in that path.
  • Wake phrases match longest-first: "hey luna what time is it" now
    strips the whole "hey luna", not just "luna" (no stray "hey" sent
    to the brain).
  • Empty recognition results keep the same listen cycle going instead
    of restarting it.
  • "listening" (blue face pulse) now means attentive conversation
    mode only; passively waiting for a wake word stays visually idle.

[core] main.py
  • voice_loop no longer drops recognised text while Luna is speaking
    (speak() serializes) — every response gets spoken.
  • Speaks a short acknowledgement on bare wake words.
  • Guards against luna_mode sticking on "processing".

[behavior] behavior_engine.py
  • Gestures can NEVER interrupt an answer: ignored entirely while
    Luna is processing or speaking (the old check missed the 1-2 s
    Groq "processing" gap — a wave there killed the answer).
  • During an open conversation window gestures react silently
    (face + servos only) so they don't talk over the user.
  • FIXED: wave/open-palm wrote state.emotion directly, which the
    vision loop overwrote within ~125 ms — the happy face never
    showed. Now uses face_override like the other reactions.

[face] robot_face.py
  • FACE STYLES — config FACE_STYLE, or press 1 / 2 on the window:
      1 = Luna classic (purple, rounded, organic mouth) — unchanged
      2 = Robo (cyan, sharp corners, square sensor pupils,
          equalizer-bar talking mouth — modern-robot look)
    Switching back to 1 restores the exact original face.
  • Conversation indicator: small breathing dot below the mouth while
    the wake window is open; on timeout the eyes dim briefly and play
    one slow blink — subtle "say the wake word again" cue.
  • Slightly brighter eye tint while the conversation window is open.
  • Pi performance: glow surfaces now cached (were rebuilt every
    frame), zzz font cached (SysFont was created per particle per
    frame), full-screen Surface allocated once instead of per frame.
  • Window close now raises SystemExit so main.py's clean shutdown
    (servo centering, GPIO release) actually runs.

[cleanup]
  • gesture_module.py — removed unused numpy + mp_drawing imports.
  • face_renderer.py  — removed unused pygame/RENDER_FPS imports.
  • brain.py          — history trim is a proper while-loop.
  • camera_thread.py  — ESP32 path now sets state.camera_ok too.
  • config.py         — removed dead WAKE_WORD; new MIC_BLOCK_AFTER_SPEAK,
                        WAKE_REPLIES, FACE_STYLE.
  • shared_state.py   — new convo_expired_time field.
  • .gitignore        — verified venv/ + .venv/ already ignored (no
                        change needed).

─── 2026-07-15 — Big robustness + expression update ──────────────

[security] config.py
  • GROQ_API_KEY no longer hardcoded — loaded from environment or
    the git-ignored .env file. Old key moved into .env.
    ⚠ The old key was previously committed in plaintext — rotate it
    at https://console.groq.com when convenient.

[project] .gitignore  (new)
  • Ignores __pycache__, .DS_Store, venv, .env, vosk model dir,
    *.pth model weights, editor folders.

[project] .env / .env.example  (new)
  • .env holds the real GROQ_API_KEY (git-ignored).
  • .env.example is the safe template to commit.

[speech] speech_to_text.py  (rewritten)
  • Persistent mic stream — opened once at startup instead of
    reopened on every listen() call → lower latency, no device
    re-open glitches between turns.
  • Auto-detects mic sample rate (MIC_SAMPLE_RATE=None in config);
    resamples any rate (44.1k/48k/32k…) to 16 kHz for Vosk via
    numpy interpolation — old code only worked with exact
    integer-multiple rates.
  • MIC NOT FOUND is now handled: Luna keeps running (face, vision,
    gestures) and retries the mic every 5 s with help printed once.
  • Multiple wake words: WAKE_WORDS = ["hello", "luna", "hey luna"],
    matched on word boundaries (old code did substring match).
  • Fixed bare `except:` in queue flush (now catches queue.Empty).
  • listen() aborts cleanly if Luna starts speaking mid-listen.

[speech] text_to_speech.py
  • macOS fallback: uses the built-in `say` command when pyttsx3
    fails (espeak doesn't exist on Mac).
  • Arm sway now repeats every 3.5 s for the whole reply instead of
    a single sway at the start.
  • No-speaker case never crashes — falls through with a console
    message.

[vision] camera_thread.py
  • CAMERA NOT FOUND handled: tries configured CAMERA_ID then
    indices 0/1/2, retries every 5 s forever, sets state.camera_ok.
  • Local camera now auto-reopens if it stops delivering frames
    (~3 s of failures) — previously the thread just died silently
    when the camera was missing at startup.

[brain] brain.py
  • Fixed greeting bug: "hi" was matched as a substring, so words
    like "this"/"chill" made Luna wave. Now whole-word matching.
  • Works fully offline when no GROQ_API_KEY is set (local knowledge
    base only) instead of crashing at import.
  • Compliments ("love you", "good job", "cute", "awesome"…) now
    trigger the new heart-eyes "love" face.

[gestures] gesture_module.py
  • New gestures from the SAME MediaPipe hand model (zero extra CPU):
      THUMBS_UP  — thumb up, fingers folded
      PEACE      — index + middle up
      OPEN_PALM  — all five fingers extended
    (existing: WAVE, POINT_LEFT/RIGHT/UP/DOWN, HAND)

[behavior] behavior_engine.py  (rewritten)
  • Thumbs up → excited face + both servo arms up + "Thank you!"
  • Peace     → excited face + right arm up + "Peace!"
  • Open palm → silent happy face
  • Per-gesture cooldown (GESTURE_REACT_COOLDOWN = 8 s) and never
    talks while Luna is speaking or listening.

[servos] servo_module.py
  • New actions: arm_up("left"/"right"), arm_down("left"/"right"),
    arms_up() (both arms celebration). All smooth-move based,
    safe in simulation mode (ENABLE_SERVOS=False).

[face] robot_face.py + face_renderer.py
  • Two new face states:
      excited — wide sparkly eyes, starbursts keep sprinkling
      love    — heart particles float up, strong blush, soft smile
  • Smooth two-phase blink (animated close AND open with easing,
    occasional double-blink) — replaces the old instant snap-shut.
  • Cute decaying bounce when Luna turns happy/excited/love.
  • Sound-wave rings from the mouth while talking re-enabled and
    tuned — nicer talking animation.
  • face_override in shared_state lets gestures/compliments drive
    the face for FACE_OVERRIDE_SECS then auto-expire.

[core] main.py
  • Clean shutdown on Ctrl+C / SIGTERM / window close: servos
    centered + GPIO released (servo.cleanup() was never called
    before) and pygame quit properly.

[core] shared_state.py
  • New fields: face_override, face_override_until, mic_ok,
    camera_ok.

[docs] README.md  (new)
  • Full setup for macOS / Windows / Raspberry Pi, architecture
    overview, troubleshooting for mic/speaker/camera not found,
    device + sample-rate configuration guide.

[config] config.py
  • AUDIO_INPUT_DEVICE default changed 1 → None (auto-detect; the
    new detection verifies devices actually work before using them).
    Set it back to an index if auto-detect picks the wrong mic.
  • MIC_SAMPLE_RATE default changed 32000 → None (auto-detect).
  • New: WAKE_WORDS, GESTURE_REACT_COOLDOWN, FACE_OVERRIDE_SECS.

══════════════════════════════════════════════════════════════════
(add new entries above this line, newest first within a date block)
